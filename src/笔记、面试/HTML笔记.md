# css

```css
position:absoulute;
float:left/right;
//会把内部元素全部转成 inline-block元素

//浮动元素会产生浮动流

//所有产生了浮动流的元素，块级元素看不到他们，产生了bfc的元素和文本类属性元素以及文本都能看到浮动元素

//如何触发bfc：
position:absoulute;
display:inline-block;
float:left/right;
overflow:hidden;
```

